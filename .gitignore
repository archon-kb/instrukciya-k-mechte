# --- сборка и зависимости ---
node_modules/
dist/
dist-ssr/
*.tsbuildinfo
*.local
logs/
*.log

# --- ОС и IDE ---
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# --- секреты ---
.env
.env.*
!.env.example

# --- внутренние слои: публичный репозиторий несёт только код и веб-ассеты ---
# знания команды (Obsidian-vault), внутренняя аналитика
знания/
# невоспроизводимые медиа-мастера (вторая копия — в ~/_backups; карта — MANIFEST.md, он В репо)
media-source/*
!media-source/MANIFEST.md
# лаборатория экспериментов
lab/
# архив-предок проекта (содержит файл с указанием на API-ключ — не публиковать)
Нужно проверить что это/
# операционка Claude: навигаторы, рамки, стилевые концепты
_*.md
.claude/
